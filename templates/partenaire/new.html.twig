{% extends 'base.html.twig' %}

{% block body %}

<title>Création d'un partenaire</title>

<h1 class="text center">Créer un partenaire</h1>
<div class="container">
    <div class="row">
        <div class="col s12">
        {{form_start(form)}}
        {{form_widget(form)}}
            <button class="btn waves-effect waves-light light-blue darken-4" type="submit" name="action">Créer le partenaire
            <i class="material-icons right">send</i></button>
        {{form_end(form)}}
        </div>
    </div>
</div>

{% endblock %}